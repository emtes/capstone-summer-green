events {}

http {
    upstream cluster {
        <%= loadBalancingMethod %>;
    <% for (let i = 0; i < clusterSize; i++) { -%>
    <%= `server ${i}` %>
    <% } -%>
    
    }

    server {
        listen 80;

        location / {
            proxy_pass http://cluster
        }
    }
}